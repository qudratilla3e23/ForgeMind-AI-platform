# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# --- Validatsiya ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# --- Ma'lumotlar bazasi (PostgreSQL) ---
sqlalchemy>=2.0.29
asyncpg>=0.29.0
alembic>=1.13.1

# --- Autentifikatsiya ---
pyjwt>=2.8.0
bcrypt>=4.0.1

# --- Redis (cache / rate-limit) ---
redis>=5.0.3

# --- HTTP client (Google OAuth tokeninfo, AI providerlar) ---
httpx>=0.27.0

# --- AI Providerlar SDK ---
openai>=1.30.0

# --- To'lov (legacy — Supabase/Stripe, Phase 2'da PostgreSQL'ga to'liq ko'chiriladi) ---
supabase>=2.4.0
stripe>=9.0.0

# --- Muhit o'zgaruvchilari ---
python-dotenv>=1.0.1
